node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example
.vercel

# Generated: esbuild bundle of geminiService for Vercel functions
api/_lib/

# Local wrangler state from `wrangler pages dev`
.wrangler/

# Firebase service account key for scripts/migrate-to-supabase.mjs.
# Full access to the Firebase project — never commit it.
service-account.json

# Exported teacher data — never commit.
backups/
